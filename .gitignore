node_modules/
dist/
build/
coverage/
.turbo/

# env: ignora qualquer .env, inclusive .env.production, e libera so os
# arquivos de exemplo, que nunca tem valor real dentro.
.env
.env.*
!.env.example
!.env.*.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# prisma
apps/api/prisma/*.db

# arquivos enviados pelo corretor no ambiente local (fotos, documentos).
# Em producao isso vive no bucket S3, nunca no repositorio.
apps/api/storage/

# vite
apps/web/dist/

# vercel cli (guarda ids de projeto e org da conta ligada nesta maquina)
.vercel
